var should = require('chai').should; 

describe('EditName', function() {
  it('should edit the name of a task in the list', function() {

    //allow user to edit the name of a task in the task list

  });
});